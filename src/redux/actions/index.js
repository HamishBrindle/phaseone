import * as types from './constants.js'

// export const fooBar = (payload) => ({
//     type: types.FOO_BAR,
//     payload
// })

export const showMenu = () => ({
    type: types.SHOW_MENU
})

export const hideMenu = () => ({
    type: types.HIDE_MENU
})